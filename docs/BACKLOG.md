# Backlog

Spor opgaver for MyCMSSolution her. Opdater status og note, når en opgave flytter sig — se `.githooks/pre-push`, som advarer, hvis kodeændringer pushes uden en tilhørende opdatering af denne fil.

**Fast regel fremover:** Enhver ny opgave får et fortløbende ID (`TASK-07`, `TASK-08` osv.) i det øjeblik den defineres — både her og i `docs/kickoff-plan-fase3-cms-mycmssolution.md`. ID'et bruges i commit-/PR-beskrivelser, så arbejdet altid kan spores tilbage til den oprindelige opgave.

| ID | Prioritet | Opgave | Status | Sprint/uge | Note |
|---|---|---|---|---|---|
| TASK-01 | Høj | Scaffold af .NET-løsningen (Web, Core, Crm.Client, SelfService, Tests) | Færdig | Uge 1-2 | Umbraco 17 LTS på .NET 10, SQLite til lokal dev. Solution, projektreferencer, build og test verificeret. Flad mappestruktur (alle projekter i repo-roden, ingen `src/`+`tests/`-opdeling) er bekræftet beslutning — kickoff-planen er opdateret til at afspejle dette. |
| TASK-02 | Høj | CRM-integrationslag isoleret bag interface, med midlertidig mock | Færdig | Uge 1-2 | `ICrmClient` (`GetCustomerProfile`, `GetSubscriptions`, `GetInvoices`) er implementeret i `MockCrmClient` med 3 eksempelkunder (2x B2C, 1x B2B, heriblandt et abonnement i binding), og dækket af 10 unit-tests. Rigtig implementering tilføjes senere som ny klasse bag samme interface. |
| TASK-03 | Mellem | Docker-opsætning (dev + prod compose) | Ikke startet | Uge 1-2 | `docker-compose.dev.yml` og `docker-compose.prod.yml` i `/docker`, jf. kickoff-planens TASK-03. Dev-filen er kun til verificeringskørsler før merge til `develop` (ikke daglig brug), prod skal hærdes (persistent storage, sikkerhedskonfig, ingen default credentials). |
| TASK-04 | Mellem | Backlog og versioneret git pre-push-hook | Færdig | Uge 1-2 | `docs/BACKLOG.md` (denne fil) + `.githooks/pre-push` (advarer, blokerer ikke, ved kodeændringer uden BACKLOG-opdatering) + opsætningsinstruktion i README (`git config core.hooksPath .githooks`). Hook testet reelt: advarer korrekt ved kodeændring uden backlog-opdatering, tier korrekt når backloggen opdateres i samme push. |
| TASK-05 | Høj | Automatiseret test-workflow ved push/PR | Færdig | Uge 1-2 | `.github/workflows/test.yml` kører `dotnet restore`/`build`/`test` (Release) på `MyCMSSolution.slnx` ved push til `main`/`develop` og ved pull request mod disse branches, på `ubuntu-latest` med .NET 10 SDK. Fejl i build eller test giver automatisk rødt kryds i GitHub, da `dotnet test` returnerer non-zero exit code. Verificeret lokalt i Release-konfiguration (build + alle 10 tests grønne) før push. |
| TASK-06 | Høj | Uafhængig kodegennemgang ved milepæle | Tilbagevendende, ikke startet | Ved hver milepæl | Manuel gennemgang i en **frisk, uafhængig Claude Code-session** (ikke den der byggede koden) — ved Fase 1-lancering, Fase 2-lancering, og altid før CRM-skrive-endpoints går i produktion. Vurderer arkitekturoverholdelse, sikkerhed (særligt autentificering/CRM-kald), ubegrundede afvigelser fra kickoff-planen, samt manglende fejlhåndtering/tests. Retter ikke selv — rapporterer kun fund, prioriteret efter alvorlighed. Jf. kickoff-planens TASK-06. |
| TASK-08 | Høj | Undersøg SQLite Error 14 ("unable to open database file") mod umbraco/Data/Umbraco.sqlite.db, som fortsat opstår efter flytning af projektmappen ud af OneDrive | Færdig | Uge 1-2 | **Rodårsag fundet og reproduceret, ikke rettet endnu (undersøgelsesopgave):** Ingen kode i løsningen kalder `AppDomain.CurrentDomain.SetData("DataDirectory", ...)` (bekræftet ved søgning i hele repoet). Umbracos `GetUmbracoConnectionString()` erstatter kun `\|DataDirectory\|`-pladsholderen, hvis denne AppDomain-værdi er sat — ellers efterlades den bogstaveligt i forbindelsesstrengen. Midlertidig diagnostik i `Program.cs` (kørt lokalt, derefter fjernet igen — ingen ændringer efterladt) bekræftede: `AppDomain.CurrentDomain.GetData("DataDirectory")` = `null`; `GetUmbracoConnectionString()` returnerer `Data Source=\|DataDirectory\|/Umbraco.sqlite.db;...` uændret; `SqliteConnection.DataSource` viser samme bogstavelige, ikke-opløste sti (et relativt "spøgelses-stinavn" med pipe-tegn, der ikke findes som mappe); direkte `SqliteConnection.Open()` mod denne streng kaster præcis `SQLite Error 14: 'unable to open database file'` — identisk med den fejl, Umbracos eget opstartslog derefter viser gentagne gange ("Configured database is reporting as not being available"). Skriveadgang til `umbraco/Data/` blev testet og virker fint (en tekstfil blev skrevet og slettet uden problemer, og mappen indeholder allerede en `TEMP/MainDom_*.lock`-fil fra tidligere kørsler) — så det er **ikke** en mappe-/rettighedsproblemstilling og **ikke** relateret til OneDrive-flytningen. Fejlen opstår kun, når `ASPNETCORE_ENVIRONMENT=Development` er sat, så `appsettings.Development.json` (som indeholder `umbracoDbDSN`) faktisk indlæses oven på den almindelige `appsettings.json` (denne findes fra TASK-01, men har ingen `ConnectionStrings`-sektion) — hverken proces-, bruger- eller maskinniveau har `ASPNETCORE_ENVIRONMENT` sat i dette miljø. **Rettelse af tidligere unøjagtighed:** en tidligere version af denne note påstod fejlagtigt at `appsettings.json` slet ikke fandtes — det skyldtes en fejl i en shell-kommandos arbejdsmappe under undersøgelsen, ikke et reelt fund; filen har eksisteret siden `10a42b0` (TASK-01). **Foreslået rettelse (ikke udført):** tilføj `AppDomain.CurrentDomain.SetData("DataDirectory", Path.Combine(builder.Environment.ContentRootPath, "umbraco", "Data"))` i `Program.cs` før `CreateUmbracoBuilder()`. **Bemærk om ID:** springer fra TASK-06 til TASK-08 — TASK-07 findes ikke i historikken; flag venligst om dette er en fejl eller en opgave defineret et andet sted. |
| TASK-09 | Høj | Ret SQLite Error 14: tilføj `AppDomain.CurrentDomain.SetData("DataDirectory", ...)` i Program.cs og bekræft at Umbraco-installationswizarden vises | Færdig | Uge 1-2 | Rettelsen fra TASK-08 er implementeret: `AppDomain.CurrentDomain.SetData("DataDirectory", Path.Combine(builder.Environment.ContentRootPath, "umbraco", "Data"))` er tilføjet i `MyCMSSolution.Web/Program.cs` før `CreateUmbracoBuilder()`. Verificeret ved `dotnet run --project MyCMSSolution.Web` (launch-profilen `Umbraco.Web.UI`, https://localhost:44367): ingen `SQLite Error 14`/"Configured database is reporting as not being available"-advarsler ved opstart (mod 5 stk. før rettelsen), `curl https://localhost:44367/` returnerer HTTP 200 med Umbracos backoffice-SPA-shell (mod HTTP 500 `BootFailedException` før rettelsen), og `GET /umbraco/management/api/v1/server/status` returnerer `{"serverStatus":"Install"}`, hvilket bekræfter at Umbraco har booted korrekt og venter på installationswizarden (som er en klient-side SPA, derfor bruges dette API-kald som definitivt bevis i stedet for at rendere JS via curl). `dotnet build`/`dotnet test` (10/10) verificeret grønne efter rettelsen. |
| TASK-07 | Høj | Scaffold Umbraco-dokumenttyper for de otte bekræftede sidetyper (Spor 2) | Færdig | Uge 1-2 | **Implementering:** Umbraco 17's `IContentTypeService` har ikke længere en `Save()`-metode til at oprette dokumenttyper (fjernet til fordel for den nye async Management-API). Sidetyperne oprettes derfor kodefirst via `IContentTypeEditingService.CreateAsync()` + `IDataTypeService` i `MyCMSSolution.Web/ContentTypes/` (`PageTypeAliases.cs`, `ConfirmedPageTypesComposer.cs`, `ConfirmedPageTypesSeeder.cs`), kørt automatisk via en `INotificationAsyncHandler<UmbracoApplicationStartedNotification>` **kun når `IRuntimeState.Level == Run`** (dvs. efter en installeret database) — idempotent, tjekker eksisterende alias'er/datatype-navne først, så den er sikker at lade køre ved hver opstart. Opretter 7 fælles datatyper (præfikset `MyCMSSolution -`, bl.a. et delt Block Grid-felt med tom bloktyper-liste, klar til det kommende blok-bibliotek) og de 8 dokumenttyper: **Produktside** (produktsegment-tags + Block Grid — én fleksibel type, ikke separate typer pr. segment), **Kampagne/tilbud** (start-/slutdato + Block Grid), **Nyhed/presse** (udgivelsesdato + Block Grid), **Driftsinfo** (kun et `apiKilde`-tekstfelt til at identificere det eksterne API/feed — selve driftsstatussen er *ikke* et Umbraco-indholdsfelt, den skal renderes live fra API'et i en Razor-visning; får stadig Block Grid-feltet, jf. kravet om at alle typer skal have det, til evt. statisk rammetekst), **FAQ** (kategori-tags + Block Grid, hvor den kommende FAQ-accordion-blok skal leve), **Jobopslag** (ansøgningslink + ansøgningsemail + Block Grid til beskrivelse), **Om/kontakt/vilkår** (kun Block Grid), **Partnerside** (partnernavn + Block Grid — bevidst ingen særlig adgangsbegrænsning ud over normal backoffice-login, som specificeret). **Verificeret reelt, ikke kun kompileret:** kørte en midlertidig unattended install (kun miljøvariabler, ingen filer ændret) for at få en rigtig SQLite-database, og forespurgte direkte i databasen efter opstart — alle 7 datatyper og alle 8 dokumenttyper med alle korrekte felter og korrekte datatype-referencer blev fundet. Genstartede derefter appen og bekræftede idempotens: alle 8 sidetyper logger "findes allerede - springer over", ingen fejl, ingen dubletter. `dotnet build`/`dotnet test` (10/10) grønne. **Ikke bygget endnu (bevidst, som aftalt):** selve blok-biblioteket til Block Grid-felterne — afventer marketings svar på blok-prioritering (Åbne spørgsmål #2, #3 i kickoff-planen). |
